# Base URL of the RouterPulse API (Phase 4). Server Components fetch
# from this directly; the browser uses it for the WebSocket connection.
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_RPC_URL=http://localhost:8899
